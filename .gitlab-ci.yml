cache:
  paths:
    - node_modules/

stages:
  - test

test:node5:
  image: node:5
  stage: test
  script:
    - npm install --loglevel=warn --spin=false
    - npm test -- --coverage-report=text-summary

test:node6:
  image: node:latest
  stage: test
  script:
    - npm install --loglevel=warn --spin=false
    - npm test -- --coverage-report=text-summary
